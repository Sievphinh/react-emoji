import React from 'react'

const EmojiRowResult = ({ title, symbol }) => {
    return (
        <div>
            <p> {title}&nbsp;{symbol} </p>        
        </div>
    )
}

export default EmojiRowResult
